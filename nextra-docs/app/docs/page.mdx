import { Callout, Steps, Tabs } from "nextra/components";

# Privacy Protocol Docs

Privacy Protocol is a relayer-first privacy middleware for EVM applications.

## Install

<Tabs items={["npm", "yarn", "pnpm"]}>
  <Tabs.Tab>
    ```bash
    npm install privacy-protocol ethers
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    yarn add privacy-protocol ethers
    ```
  </Tabs.Tab>
  <Tabs.Tab>
    ```bash
    pnpm add privacy-protocol ethers
    ```
  </Tabs.Tab>
</Tabs>

<Callout type="info" emoji="⚡">
  No manual circuit import is required. The SDK ships with bundled proving artifacts.
</Callout>

## What Is Included

- `privacy-protocol/core`: low-level SDK APIs.
- `privacy-protocol/hooks`: React hooks for private app integrations.
- Built-in relayer defaults so relayer config is optional.

## Quick Flow

<Steps>
### Initialize
Instantiate with provider + pool address only.

### Deposit
Create a private note (`secret`, `nullifier`, `commitment`) with `deposit`.

### Execute or Withdraw
Spend notes privately with `executeAction` or `withdraw`.

### Track Lifecycle
Render relay progress from `queued` to `submitted` to `confirmed`.
</Steps>

## Next

- [Getting Started](/docs/getting-started)
- [Core SDK](/docs/core-sdk)
- [React Hooks](/docs/hooks)
- [Deployments](/docs/deployments)
- [Run a Relayer Node](/docs/operators/run-relayer-node)
